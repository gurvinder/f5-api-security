{{- if .Values.suggestedQuestions }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "f5-ai-security.fullname" . }}-suggested-questions
  labels:
    {{- include "f5-ai-security.labels" . | nindent 4 }}
data:
  RAG_QUESTION_SUGGESTIONS: |
    {{- .Values.suggestedQuestions | toJson | nindent 4 }}
{{- end }}

